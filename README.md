# CoreERP

Enterprise Resource Planning application — central transactional backbone for products, inventory batches, procurement, markdowns and financial impact.

## Development

```bash
npm install
npm run dev
```

The app runs on http://localhost:8080.

## Production (Docker)

```bash
docker compose up -d --build
```

The app will be available on http://localhost:6002.
